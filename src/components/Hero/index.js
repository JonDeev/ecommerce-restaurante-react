import React from 'react'
import { Navbar } from '../Navbar'
import {HeroContainer} from './HeroElements'

export const Hero = () => {
  return (
    <HeroContainer>
        <Navbar />
    </HeroContainer>
  )
}
